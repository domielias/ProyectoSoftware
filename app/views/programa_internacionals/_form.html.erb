<%= form_with(model: programa_internacional, local: true) do |form| %>
  <% if programa_internacional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(programa_internacional.errors.count, "error") %> prohibited this programa_internacional from being saved:</h2>

      <ul>
        <% programa_internacional.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <%= form.fields_for :pais do |pis| %>
    <%= pis.label :pai %>
    <%= pis.collection_select :pai_id, Pai.order(:nombre),:id,:nombre, include_blank: true%>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
