<%= form_with(model: pai, local: true) do |form| %>
  <% if pai.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pai.errors.count, "error") %> prohibited this pai from being saved:</h2>

      <ul>
        <% pai.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :nacionalidad %>
    <%= form.text_field :nacionalidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
