<%= form_with(model: programa_epe_solicitado, local: true) do |form| %>
  <% if programa_epe_solicitado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(programa_epe_solicitado.errors.count, "error") %> prohibited this programa_epe_solicitado from being saved:</h2>

      <ul>
        <% programa_epe_solicitado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
