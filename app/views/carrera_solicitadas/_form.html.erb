<%= form_with(model: carrera_solicitada, local: true) do |form| %>
  <% if carrera_solicitada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrera_solicitada.errors.count, "error") %> prohibited this carrera_solicitada from being saved:</h2>

      <ul>
        <% carrera_solicitada.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
