<%= form_with(model: modalidad, local: true) do |form| %>
  <% if modalidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modalidad.errors.count, "error") %> prohibited this modalidad from being saved:</h2>

      <ul>
        <% modalidad.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
